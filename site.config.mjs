// ─────────────────────────────────────────────────────────────────────────
// Configuración del sitio — fuente única de verdad.
//
// BASE_URL es el origen absoluto del sitio. Lo consumen los metadatos sociales
// (og:url, og:image, twitter:image) a través de `npm run build:urls`
// (scripts/build-urls.mjs). Se escribe SIN barra final.
//
// Cambiar este valor por https://achetiq.org.ar cuando se conecte el dominio final
// ─────────────────────────────────────────────────────────────────────────
export const BASE_URL = "https://achetiq.github.io";
